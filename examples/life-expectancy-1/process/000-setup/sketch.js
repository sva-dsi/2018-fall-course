/**
 * step 0: setup your workspace
 */


function setup() {
	// 0.1 create a fixed canvas size
	createCanvas(600, 600);
}

function draw() {
	// 0.1 add in a greyish background for now
	background(200, 200, 200);

}
